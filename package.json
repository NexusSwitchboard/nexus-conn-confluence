{
  "name": "@nexus-switchboard/nexus-conn-confluence",
  "version": "0.0.1",
  "keywords": [
    "nexus-connection"
  ],
  "description": "Provides a nexus compatibility layer between a module and a confluence instance.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/**/*"
  ],
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc -w",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "assert": "^2.0.0",
    "debug": "^4.1.1",
    "ts-confluence-client": "0.0.19",
    "tslib": "^1.10.0",
    "@nexus-switchboard/nexus-extend": "^0.4"
  },
  "devDependencies": {
    "@types/node": "^12.7.5",
    "@types/debug": "^4.1.5",
    "typescript": "^3.7.5",
    "tslint": "^6.0.0"
  }
}
